@page
@model Biberg.MyPortfolio.Pages.Skill.IndexModel

@{
    ViewData["Title"] = "Index";
    ViewData["DefaultAcoes"] = "Ações";

    Layout = "~/Views/Shared/_Layout.cshtml";
}
<section class="content-header">
    <h1>
        Habilidade
        <small>Nunca desista!</small>
    </h1>
</section>

<section class="content">
    <div class="box">
        <div class="box-header with-border">
            <h3 class="box-title">Habilidades Listar</h3>

            <div class="box-tools pull-right">
                <button type="button" class="btn btn-box-tool" data-widget="collapse" data-toggle="tooltip"
                        title="Collapse">
                    <i class="fa fa-minus"></i>
                </button>
                <button type="button" class="btn btn-box-tool" data-widget="remove" data-toggle="tooltip" title="Remove">
                    <i class="fa fa-times"></i>
                </button>
            </div>
        </div>
        <div class="box-body">
            <div class="box-body table-responsive no-padding">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.Skill[0].Name)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Skill[0].Score)
                            </th>
                            <th style="text-align:right">@ViewData["DefaultAcoes"]</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.Skill)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Name)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Score)
                                </td>
                                <td align="right">
                                    <a asp-page="./Edit" asp-route-id="@item.ID" title="Editar item"><i class="fa fa-edit fa-2x"></i></a> |
                                    <a asp-page="./Details" asp-route-id="@item.ID"><i class="fa fas fa-info fa-2x"></i></a> |
                                    <a asp-page="./Delete" asp-route-id="@item.ID"><i class="fa fa-remove fa-2x text-danger"></i></a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>

        </div>
        <div class="box-footer">
            <a asp-page="./Create"><i class="btn btn-success fa fa-plus fa-2x text-success"></i></a>
        </div>
    </div>
</section>

@section Scripts {
    <script src="~/plugins/input-mask/jquery.inputmask.js"></script>
    <script src="~/plugins/input-mask/jquery.inputmask.date.extensions.js"></script>
    <script src="~/plugins/input-mask/jquery.inputmask.extensions.js"></script>
    <script>
        $(function () {
            $('[data-mask]').inputmask();
        });
    </script>
}
